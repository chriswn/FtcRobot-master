package org.firstinspires.ftc.teamcode;

import org.opencv.core.Scalar;

public class BlueColorDetection {

    // Blue color thresholds
    public static final Scalar lowerBlue = new Scalar(100, 100, 100);  // Lower bound for blue color
    public static final Scalar upperBlue = new Scalar(140, 255, 255);  // Upper bound for blue color
}
