package org.firstinspires.ftc.teamcode;

import org.opencv.core.Scalar;

public class RedColorDetection {

    // Red color thresholds
    public static final Scalar lowerRed = new Scalar(0, 100, 100);  // Lower bound for red color
    public static final Scalar upperRed = new Scalar(10, 255, 255);  // Upper bound for red color
}
